alias rmrf='rm -rf'
alias pwdcp="pwd | tr -d '\n' | xclip -sel clip"

function copyclip() {
    if [[ "$1" == "-d" ]]; then
        shift 1
        "$@" | tr -d '\n' | xclip -sel clip
    else
        "$@" | xclip -sel clip
    fi
}

